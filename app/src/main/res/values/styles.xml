<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="listText">
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:layout_weight">0.04</item>
        <item name="android:fontFamily">cursive</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="applyText">
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">cursive</item>
        <item name="android:gravity">center</item>
    </style>


    <!--.-->
    <!--.-->
    <!--Estilo activity_main-->
    <!--.-->
    <!--.-->

    <style name="AppTheme.BotonCategoria" parent="TextAppearance.AppCompat.Headline">
        <!--<item name="android:padding">25dp</item>-->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">fill_horizontal</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:layout_margin">@dimen/MarginPrincipal</item>
        <item name="android:layout_weight">0.2</item>
    </style>

    <style name="AppTheme.BotonCategoria.Transparente" parent="AppTheme.BotonCategoria">
        <item name="android:background">#00537D</item>
    </style>
</resources>
